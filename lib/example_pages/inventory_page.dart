// This file stores design notes so the app code in lib/pages/inventory_page.dart
// can stay compile-safe. Keep non-Dart mockups outside lib/, e.g. in docs/.

const String inventoryPageDesignReference =
    'Inventory web mock moved from this Dart file to avoid analyzer errors.';